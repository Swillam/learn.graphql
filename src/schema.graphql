### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Comment {
  User: User
  body: String
  id: Int
}

"""
A date string, such as 2007-12-03, compliant with the `full-date` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar Date

type Mutation {
  createComment(authorId: Int!, body: String!): Comment!
  createPost(authorId: Int!, body: String!, title: String!): Post!
  createUser(email: String!): User!
  deleteComment(id: Int!): Comment!
  deletePost(id: Int!): Post!
  deleteUser(id: Int!): User!
  updateComment(body: String!, id: Int!): Comment!
  updatePost(body: String!, id: Int!, title: String!): Post!
  updateUser(email: String!, id: Int!, password: String!, username: String!): User!
}

type Post {
  author: User
  body: String
  comment: Comment
  createdAt: Date
  id: Int
  title: String
  updatedAt: Date
}

type Query {
  getAllComment: [Comment]!
  getAllPost: [Post]!
  getAllUser: [User]!
  getComment(id: Int!): [Comment]!
  getPost(id: Int!): [Post]!
  getUser(id: Int!): [User]!
}

type User {
  Post: [Post]
  email: String
  id: Int
  password: String
  username: String
}
